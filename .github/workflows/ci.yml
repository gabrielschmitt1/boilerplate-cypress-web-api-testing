name: ci

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  api-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Run API tests 🧪
        uses: cypress-io/github-action@v5
        with:
          start: npm run prereport
          command: npm run cypress:ci:stg
          #end: npm run postreport